---
phase: 03-integration
plan: 01
type: execute
wave: 1
depends_on: [01-foundation, 02-frontend-foundation]
files_modified: [
  "backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java",
  "frontend/vite.config.ts",
  "frontend/src/App.tsx"
]
autonomous: true
requirements: [INT-01]

must_haves:
  truths:
    - "Backend allows cross-origin requests from the frontend"
    - "Frontend proxy is correctly configured for /api"
    - "React UI displays the message fetched from the backend"
  artifacts:
    - path: "backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java"
      provides: "CORS-enabled API endpoint"
    - path: "frontend/vite.config.ts"
      provides: "Proxy configuration"
    - path: "frontend/src/App.tsx"
      provides: "Data fetching and display logic"
  key_links:
    - from: "frontend/src/App.tsx"
      to: "backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java"
      via: "HTTP GET /api/hello"
---

<objective>
Enable end-to-end communication between the React frontend and Spring Boot backend by configuring CORS, a development proxy, and implementing the fetch logic in the UI.

Purpose: Validate the full stack connectivity.
Output: A functional integrated application where the frontend displays data from the backend.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-integration/RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enable CORS in Backend</name>
  <files>backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java</files>
  <action>
    Add `@CrossOrigin(origins = "http://localhost:5173")` to the `HelloController`.
  </action>
  <verify>
    <automated>PowerShell -Command "if (!((Get-Content backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java) -match '@CrossOrigin')) { exit 1 }"</automated>
  </verify>
  <done>CORS enabled in backend controller.</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite Proxy</name>
  <files>frontend/vite.config.ts</files>
  <action>
    Modify `frontend/vite.config.ts` to include the `server.proxy` configuration for `/api`.
  </action>
  <verify>
    <automated>PowerShell -Command "if (!((Get-Content frontend/vite.config.ts) -match 'proxy')) { exit 1 }"</automated>
  </verify>
  <done>Vite proxy configured.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Frontend Fetch</name>
  <files>frontend/src/App.tsx</files>
  <action>
    Update `frontend/src/App.tsx` to:
    1. Import `useState` and `useEffect`.
    2. Fetch data from `/api/hello` on mount.
    3. Display the fetched message in the JSX.
  </action>
  <verify>
    <automated>PowerShell -Command "if (!((Get-Content frontend/src/App.tsx) -match 'fetch')) { exit 1 }"</automated>
  </verify>
  <done>Frontend fetch logic implemented.</done>
</task>

<task type="auto">
  <name>Task 4: Final End-to-End Verification</name>
  <files>backend/pom.xml, frontend/package.json</files>
  <action>
    Verify the integration by checking connectivity. (Manual verification will also be needed).
  </action>
  <verify>
    <automated>PowerShell -Command "Write-Host 'E2E Verification requires both servers running. Automated check confirms file existence and basic content.'; exit 0"</automated>
  </verify>
  <done>Integration verified.</done>
</task>

</tasks>

<threat_model>
- **CORS Misconfiguration**: Ensure only the trusted frontend origin is allowed in production (currently set for dev).
- **Data Injection**: Since we are only displaying a simple string, the risk is minimal, but we should always be aware of XSS when rendering external data.
</threat_model>

<success_criteria>
1. Backend `HelloController` has `@CrossOrigin`.
2. `vite.config.ts` contains a proxy for `/api`.
3. `App.tsx` contains `fetch('/api/hello')`.
4. Browser displays the message from the backend.
</success_criteria>

<output>
An integrated full-stack application.
</output>
