---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "backend/pom.xml",
  "backend/src/main/java/com/gsd/mi_primer_project_gsd/BackendApplication.java",
  "backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java",
  "backend/src/main/resources/application.properties",
  "backend/.mvn/wrapper/maven-wrapper.properties",
  "backend/mvnw.cmd"
]
autonomous: true
requirements: [BACKEND-01, STRUC-01, GSD-01]

must_haves:
  truths:
    - "The backend directory contains a valid Maven/Spring Boot structure"
    - "The application compiles and packages successfully"
    - "The /api/hello endpoint returns a success message when running"
    - "The /actuator/health endpoint is accessible and 'UP' when running"
  artifacts:
    - path: "backend/pom.xml"
      provides: "Project configuration with Java 21 and Spring Boot 3.x"
    - path: "backend/.mvn/wrapper/maven-wrapper.properties"
      provides: "Maven wrapper configuration"
    - path: "backend/mvnw.cmd"
      provides: "Windows Maven wrapper script"
    - path: "backend/src/main/java/com/gsd/mi_primer_project_gsd/BackendApplication.java"
      provides: "Application entry point"
  key_links:
    - from: "backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java"
      to: "Spring Web"
      via: "Annotations (@RestController)"
---

<objective>
Establish the backend project structure using Java 21 and Spring Boot 3.x, ensuring the application can be built, run, and responds to health checks.

Purpose: Provide a solid, verifiable foundation for backend development on Windows/PowerShell.
Output: A working Spring Boot application in the `backend/` directory with Maven wrapper and health endpoints.
</objective>

<execution_context>
@$HOME/.gemini/get-shit-done/workflows/execute-plan.md
@$HOME/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Maven Project and Configuration</name>
  <files>backend/pom.xml, backend/src/main/resources/application.properties</files>
  <action>
    1. Create the `backend` directory.
    2. Create `backend/pom.xml` with:
       - Parent: `org.springframework.boot:spring-boot-starter-parent:3.3.0`.
       - Java version: 21.
       - Dependencies: `spring-boot-starter-web`, `spring-boot-starter-actuator`, `lombok`.
       - Plugin: `spring-boot-maven-plugin`.
    3. Create `backend/src/main/resources/application.properties` and add `management.endpoints.web.exposure.include=health,info`.
    4. Setup Maven Wrapper:
       - Attempt to run `mvn wrapper:wrapper -Dmaven=3.9.6` inside `backend`.
       - If `mvn` is unavailable, create `backend/.mvn/wrapper/maven-wrapper.properties` with `distributionUrl=https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.9.6/apache-maven-3.9.6-bin.zip`.
  </action>
  <verify>
    <automated>PowerShell -Command "if (!(Test-Path backend/pom.xml)) { exit 1 }; if (!((Get-Content backend/pom.xml) -match '21')) { exit 1 }"</automated>
  </verify>
  <done>Backend structure and build configuration created with Java 21 and Spring Boot dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Application and HelloController</name>
  <files>
    backend/src/main/java/com/gsd/mi_primer_project_gsd/BackendApplication.java,
    backend/src/main/java/com/gsd/mi_primer_project_gsd/controller/HelloController.java
  </files>
  <action>
    1. Create package directory structure: `backend/src/main/java/com/gsd/mi_primer_project_gsd/controller`.
    2. Create `BackendApplication.java` in `com.gsd.mi_primer_project_gsd` with `@SpringBootApplication` and a main method calling `SpringApplication.run`.
    3. Create `HelloController.java` in `com.gsd.mi_primer_project_gsd.controller` with `@RestController` and a `@GetMapping("/api/hello")` returning "Hello from GSD Backend!".
  </action>
  <verify>
    <automated>PowerShell -Command "if (!(Test-Path backend/src/main/java/com/gsd/mi_primer_project_gsd/BackendApplication.java)) { exit 1 }; if (!((Get-Content backend/src/main/java/com/gsd/mi_primer_project_gsd/BackendApplication.java) -match '@SpringBootApplication')) { exit 1 }"</automated>
  </verify>
  <done>Application entry point and sample controller implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Verify Build, Wrapper and Runtime</name>
  <files>backend/.mvn/wrapper/maven-wrapper.properties, backend/mvnw.cmd, backend/.mvn/wrapper/maven-wrapper.jar</files>
  <action>
    1. Ensure Maven wrapper completeness:
       - Check if `backend/mvnw.cmd` and `backend/.mvn/wrapper/maven-wrapper.jar` exist.
       - If missing, use `mvn wrapper:wrapper` or download the wrapper jar manually via PowerShell.
    2. Build the project: Run `.\mvnw.cmd clean package` inside `backend`.
    3. Verify runtime:
       - Start app: `$process = Start-Process -FilePath "backend\mvnw.cmd" -ArgumentList "spring-boot:run" -WorkingDirectory "backend" -NoNewWindow -PassThru`.
       - Wait for start: `Start-Sleep -Seconds 30`.
       - Test Health: `Invoke-RestMethod -Uri "http://localhost:8080/actuator/health"`.
       - Test Hello: `Invoke-RestMethod -Uri "http://localhost:8080/api/hello"`.
       - Stop app: `Stop-Process -Id $process.Id -Force`.
  </action>
  <verify>
    <automated>PowerShell -Command "if (!(Test-Path backend/mvnw.cmd)) { exit 1 }; $p = Start-Process -FilePath 'backend\mvnw.cmd' -ArgumentList 'spring-boot:run' -WorkingDirectory 'backend' -NoNewWindow -PassThru; $success = $false; for ($i=0; $i -lt 20; $i++) { Start-Sleep -Seconds 5; try { $h = Invoke-RestMethod -Uri 'http://localhost:8080/actuator/health' -ErrorAction SilentlyContinue; if ($h.status -eq 'UP') { $success = $true; break } } catch {} }; Stop-Process -Id $p.Id -Force; if (!$success) { exit 1 }"</automated>
  </verify>
  <done>Project builds successfully with Maven Wrapper and passes runtime health checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| External -> API | HTTP requests from clients enter the system at Controller endpoints |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Information Disclosure | Actuator Endpoints | mitigate | Limited exposure to health/info in application.properties |
| T-01-02 | Tampering | pom.xml dependencies | mitigate | Use Spring Boot Parent BOM for version management |
| T-01-SC | Tampering | Maven Wrapper | mitigate | Verify wrapper properties points to official Apache distribution |
</threat_model>

<verification>
1. Check `backend/pom.xml` exists and has Java 21.
2. Check `backend/mvnw.cmd` and `.mvn/wrapper` files exist.
3. Run `.\mvnw.cmd clean package` in `backend/` and confirm "BUILD SUCCESS".
4. Confirm `Invoke-RestMethod http://localhost:8080/actuator/health` returns status UP while app is running.
</verification>

<success_criteria>
- `backend/` exists and contains a valid Maven project.
- `backend/mvnw.cmd` and `.mvn/wrapper/` are present and functional.
- Project compiles and packages without errors.
- `/api/hello` returns "Hello from GSD Backend!".
- `/actuator/health` returns status "UP".
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
